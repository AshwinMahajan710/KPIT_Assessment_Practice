from BatteryPack import BatteryPack

class EVCar:
    def __init__(self):
        car_seat_count: int # which is an integer (must be positive non-zero value)
        car_price: int # which is a non-zero, positive integer
        car_battery_pack: BatteryPack  # which is an instance of type BatteryPack (must NOT BE NULL)
        pass
    